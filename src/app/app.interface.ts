export interface PriceData {
  price: string;
  color: string;
}

export interface PlateLog {
  price: string;
  color: string;
  amount: number;
}

export interface PriceAndColor {
    price: string;
    color: string;
}